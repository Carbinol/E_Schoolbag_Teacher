<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teacherebag">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CheckTWList.DecryptActivity"></activity>
        <activity android:name=".CheckTWList.CheckTWActivity" />
        <activity android:name=".NewTeacherWork.EditLabelActivity" />
        <activity android:name=".NewTeacherWork.ChooseLabelActivity" />
        <activity android:name=".NewTeacherWork.NewTeacherWorkActivity" />
        <activity android:name=".NewTeacherWork.CourseListTeacherWorkActivity" />
        <activity android:name=".CheckStudentList.StudentListActivity" />
        <activity android:name=".CheckStudentList.TeacherCourseListActivity" />
        <activity android:name=".User.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>